# Notelert Plugin v1.1.7

## Maintenance

- Removes the legacy `notelertApiKey` setting from the plugin settings type.
- Publishes the current Obsidian review branch from the official plugin repository.
- Keeps the release assets limited to `main.js`, `manifest.json`, and `styles.css`.

## Release assets

- `main.js`
- `manifest.json`
- `styles.css`
